[phases.setup]
nixPkgs = ["python311", "ffmpeg-full"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m playwright install-deps chromium && python -m playwright install chromium && uvicorn app_demo:app --host 0.0.0.0 --port $PORT"
